{
  "name": "ahmarius-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "admin": "./start.sh",
    "admin:dev": "npm --prefix admin-app run tauri dev",
    "admin:test": "npm --prefix admin-app test && cargo test --manifest-path admin-app/src-tauri/Cargo.toml",
    "build": "node scripts/build-site.mjs",
    "dev": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "projects:migrate": "node scripts/projects-db.mjs migrate",
    "projects:build": "node scripts/projects-db.mjs build",
    "lint": "node scripts/lint.mjs",
    "test": "node scripts/lint.mjs && node --test scripts/test/"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "katex": "0.18.5",
    "marked": "^18.0.11"
  }
}
